name: Mobile Checks

on:
  pull_request:
    paths:
      - apps/mobile/**
      - packages/**
  push:
    branches:
      - main
      - dev
    paths:
      - apps/mobile/**
      - packages/**

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    permissions:
      checks: write
      contents: read
      pull-requests: read
      statuses: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.0
      - uses: ./.github/actions/pnpm
        with:
          after-install: 'false'
      - name: Run lint
        run: pnpm --filter @safe-global/mobile lint

  prettier:
    permissions:
      checks: write
      contents: read
      pull-requests: read
      statuses: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.0
      - uses: ./.github/actions/pnpm
        with:
          after-install: 'false'
      - name: Run Prettier check
        run: pnpm --filter @safe-global/mobile prettier

  type-check:
    permissions:
      checks: write
      contents: read
      pull-requests: read
      statuses: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.0
      - uses: ./.github/actions/pnpm
        with:
          after-install: 'false'
      - name: Type check
        run: pnpm --filter @safe-global/mobile type-check
